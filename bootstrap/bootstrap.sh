#!/bin/bash

echo "boostrapping needed packages; you may be asked to authenticate for sudo"
sudo apt install aptitude ansible

git config --global user.email "buzzdavidson@me.com"
git config --global user.name "Steve Davidson"


